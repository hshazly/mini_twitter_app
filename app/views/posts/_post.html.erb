<strong> <%= get_post_owner(post.user_id) %></strong> <br>
<%= post.context %> <br><br>

<%= link_to "", user_post_path(@user.id, post.id), :method => :delete, :class => "icon-trash", data: {confirm: "Are you sure you want to delete this post?"} %> 

<%= link_to "", user_post_path(@user.id, post.id), :method => :get, :class => "icon-eye-open", :style => "padding-left: 10px" %>
<hr>

