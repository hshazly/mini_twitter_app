<div class="row">
  <div class="span6 offset4">
      <table class="table table-bordered">
          <tr><td>
              <strong><%= @user.email %></strong> <br><br>
              <%= @post.context %> <br>
              <%= link_to "", user_post_path(@user.id, @post.id), :method => :delete, :class => "icon-trash", data: {confirm: "Are you sure you want to delete this post?"} %> 
          </td></tr>
      </table>
  </div>
</div>
